# Convex Functions

This directory contains the Convex backend for Better Auth.

Run `pnpm convex:dev` from the project root to configure a deployment, push these
functions, and generate local env values.
